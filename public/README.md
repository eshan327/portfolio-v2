Required static files for this portfolio:

- noise.png (200x200 grayscale noise texture used by src/app/globals.css)
- resume.pdf (linked from the Hero section)

Add both files to this directory before final deployment.